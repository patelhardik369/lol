# Dependencies for the Polymarket BTC 5-minute maker bot.
#
# PAPER / DRY_RUN NEEDS NOTHING INSTALLED.
# The read paths (Binance klines, Polymarket Gamma/CLOB market data) and the
# paper-trade path use ONLY the Python standard library (urllib), so
# `python main.py --dry-run` runs on a bare Python 3.9+ interpreter.

# Optional convenience (DRY_RUN): load secrets/overrides from a local .env file.
python-dotenv>=1.0

# LIVE only (Phase 5): official Polymarket CLOB **V2** client.
#   PyPI:  py-clob-client-v2    import:  py_clob_client_v2    Python >= 3.9.10
#   This REPLACES the legacy `py-clob-client` (Polymarket asks users to migrate
#   to V2). It pulls httpx[http2], eth-account, poly_eip712_structs, py-order-utils.
py-clob-client-v2>=1.0.1

# Optional (research/backtests in scripts/, Phase 5):
# pandas>=2.0
